//--------- createPool 메소드를 사용할 수 있도록 promise 적용 ---------------------------------------
    // 콜백 방식보다 promise 객체를 사용하여, async / await 방식이 직관적이고 편리하다.
const mysql2 = require("mysql2/promise");
//-------------------------------------------------------------------------------------------------

//--------- Mysql 연결 진행 ------------------------------------------------------------------------
const mysql = mysql2.createPool({
    user : "root",
    password : "980214",
    multipleStatements : true,
    database : "toy_postPage"
})
//-------------------------------------------------------------------------------------------------

//--------- Mysql 연결확인 메소드 ------------------------------------------------------------------
mysql.getConnection((err,res)=>{
    console.log(err);
})
//-------------------------------------------------------------------------------------------------

//--------- Mysql의 정보를 담아 내보낸다. (users.js / posts.js) ------------------------------------
module.exports = mysql;
//-------------------------------------------------------------------------------------------------
